import 'package:flutter/material.dart';

class NavigationConstants {
  static const Duration swipeAnimationDuration = Duration(milliseconds: 100);
  static const Curve swipeAnimationCurve = Curves.easeIn;
}
